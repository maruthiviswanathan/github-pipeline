kind: Module
type: service
name: spring-boot-app
build:
  type: docker
  dockerfile: Dockerfile
services:
  - name: spring-boot
    ports:
      - "8081:8081"
    healthCheck:
      path: "/actuator/health"
